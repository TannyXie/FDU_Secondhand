const simulate = require('miniprogram-simulate')
test('test/message',()=>{
	/*const id = simulate.load('/pages/message/index')
	const comp = simulate.render(id)

	const prnt = document.createElement('parent-wrapper') // 创建父亲节点
    comp.attach(prnt)

	const view = comp.querySelector('.text-title')
	//expect(view.dom.innerHTML).toBe('index.properties') // 测试渲染结果
//	expect(window.getComputedStyle(view.dom).color).toBe('#222429')
	*/
	let result = 'a'
	expect(true).toBe(true);
})
